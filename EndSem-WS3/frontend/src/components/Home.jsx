import { Typography } from "@mui/material";

const Homepage = ()=>
{   console.log("Homepage")
    return(
        <>
        <h2>Welcome to Form Validation using Material UI</h2>
        <Typography>
            In this experiment we are using Material UI to validate form fields.<br />
            It means checking whether the input fields entered in form are valid or not.
        </Typography>
        </>
    )
}

export default Homepage;